server:
  port: 9000
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/maheshvira-git/visitor-management-config-server
          searchPaths: api-config-properties